type Notification {
  id: String!
  name: String!
  number: String
  email: String
}

input CreateNotificationInput {
  name: String!
  number: String
  email: String
}

type Query {
  getNotification: [Notification]
}

type Mutation {
  addPerson(persNotificationon: CreateNotificationInput): Notification
}
